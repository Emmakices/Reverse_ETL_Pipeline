# Airflow Docker Compose environment variables
# Copy this file to .env and fill in production values:
#   cp .env.example .env

# ── PostgreSQL (Airflow metadata DB) ──
POSTGRES_USER=airflow
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=airflow

# ── Airflow admin user ──
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=change_me_in_production

# ── Project root (absolute path to the Reverse ETL repo) ──
# Required: Docker bind-mounts need the absolute path on Windows
# (relative '..' can silently fail when the path contains spaces)
PROJECT_ROOT=C:/Users/User/Desktop/Reverse_ ETL

# ── Ecom API (data source) ──
ECOM_API_DB=EcomRawDB
ECOM_API_DB_USER=api_reader
ECOM_API_DB_PASSWORD=change_me_in_production
ECOM_API_KEY=change_me_in_production

# ── Azure CLI auth ──
# Your Windows user profile path (used to mount ~/.azure into containers)
# On Windows this is typically C:/Users/YourUsername
USERPROFILE=C:/Users/User
